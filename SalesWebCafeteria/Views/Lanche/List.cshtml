@model IEnumerable<SalesWebCafeteria.Models.Lanche>


<div>
    <h1>@ViewData["Titulo"]</h1>
    <h5>@ViewData["Data"]</h5>
</div>
<hr />
<h5>@ViewBag.Total @ViewBag.totalLanches</h5>
<hr />
<div class="Container">
    <div class="row">
        @foreach (var lanche in Model)
        {
            <div class="col-sm-4 col-md-4 col-lg-4">
                <h3>@lanche.Nome</h3>
                <p><img src="@lanche.ImagemUrl" class="img-fluid"></p>
                <h5>Valor: R$@lanche.Preco.ToString("F2", CultureInfo.InvariantCulture)</h5>
                <p>
                    <h6>Ingredientes: <br/>@lanche.DescricaoCurta</h6>
                </p>
            </div>
        }
    </div>
</div>